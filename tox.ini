[tox]
envlist = py34

[testenv]
deps = pytest
commands = py.test

[pytest]
addopts =
    --doctest-modules
    --strict
    --ignore=setup.py
    --ignore=run_script.py
    --ignore=versioneer.py
    --ignore=OpenPNM_install.py

norecursedirs =
    .git
    .tox
    examples
    tests
    docs
    "article recreation"
    LocalFiles
